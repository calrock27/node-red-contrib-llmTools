# node-red-contrib-llmTools
This is a Node-Red node package that allows the user to create/manage custom actions via a shell and more easily expose their environment to an LLM in a structured manner.
